<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flash.framework.order.sharding.mapper.OrderMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.flash.framework.order.sharding.model.entity.Order">
        <id column="id" property="id" />
        <result column="deleted" property="deleted" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="tenant_id" property="tenantId" />
        <result column="buyer_id" property="buyerId" />
        <result column="buyer_name" property="buyerName" />
        <result column="shop_name" property="shopName" />
        <result column="shop_id" property="shopId" />
        <result column="pay_status" property="payStatus" />
        <result column="delivery_status" property="deliveryStatus" />
        <result column="receive_status" property="receiveStatus" />
        <result column="reverse_status" property="reverseStatus" />
        <result column="pay_at" property="payAt" />
        <result column="shipping_at" property="shippingAt" />
        <result column="confirm_at" property="confirmAt" />
        <result column="paid_amount" property="paidAmount" />
        <result column="sku_origin_total_amount" property="skuOriginTotalAmount" />
        <result column="sku_discount_total_amount" property="skuDiscountTotalAmount" />
        <result column="ship_fee_origin_amount" property="shipFeeOriginAmount" />
        <result column="ship_fee_discount_total_amount" property="shipFeeDiscountTotalAmount" />
        <result column="delivery_address" property="deliveryAddress" />
        <result column="created_by" property="createdBy" />
        <result column="updated_by" property="updatedBy" />
        <result column="accomplish_at" property="accomplishAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        deleted,
        created_at,
        updated_at,
        id, tenant_id, buyer_id, buyer_name, shop_name, shop_id, pay_status, delivery_status, receive_status, reverse_status, pay_at, shipping_at, confirm_at, paid_amount, sku_origin_total_amount, sku_discount_total_amount, ship_fee_origin_amount, ship_fee_discount_total_amount, delivery_address, created_by, updated_by, accomplish_at
    </sql>

</mapper>
